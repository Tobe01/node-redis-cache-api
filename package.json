{
  "name": "redis-cache-api",
  "version": "1.0.0",
  "description": "A simple Node.js + Express API with Redis caching",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["node", "express", "redis", "api", "cache"],
  "author": "Tobe",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "redis": "^5.8.2"
  }
}

